import InputLabel from "../UI/InputLabel";

const RestaurantList = () => {
  return (
    <InputLabel label="Ресторан*" placeholder="Выберите ресторан" name="dev" />
  );
};

export default RestaurantList;
